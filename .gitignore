# quilt working directory:
/.pc

/dist/
/tmp/

# After Effects rotates these every few minutes, so tracking even one of them
# guarantees a permanently dirty tree. The .aep projects themselves ARE tracked.
Adobe After Effects Auto-Save/

# AE writes a render-log folder next to the project on every queue render.
*.aep Logs/
